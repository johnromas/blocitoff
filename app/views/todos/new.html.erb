<%= form_for [@list, @todo] do |f| %>
  <div class='form-group'>
    <%= f.label 'Task Name' %>
    <%= f.text_field :desc, class: 'form-control', placeholder: 'Enter a task!' %>
  </div>
  <div class='form-group'>
    <%= f.submit 'Save', class: 'btn btn-success' %>
  </div>
<% end %>